<Window x:Class="Proba_Wisual.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MainWindow.Views.Windows"
        mc:Ignorable="d"
        Name="windowMain"
      Title="MainWindow" Height="700" Width="1000">
    <ComboBox x:Name="ComboBoxWM"  Width="360" FontSize="18" VerticalContentAlignment="Center" Margin="0,5,0,5"
        HorizontalContentAlignment="Center"
        ItemSource="{Binding Patch=DetalCollection}"
        SelectedItem="{Binding SelectedDetal, Mode=TwoWay}"
        SelectedValuePatch="Nomer"
        SelectedIndex="0">
        <ComboBox.ItemTemplate>

            <DataTemplate>
                <TextBlock Text="{Binding Name}"/>
            </DataTemplate>

        </ComboBox.ItemTemplate>


    </ComboBox>

    <Grid>
        <Image Source="C:\Users\Orobej\Desktop\PLITA_REBRA_PROG\Screenshot_10.png"/>
        <Canvas Background="Lavender">
            <Button Background="AliceBlue" Content="ImageCancelUri" Canvas.Top="20" Canvas.Left="40" />
            <Button Background="Aquamarine" Content="Bottom 30 Left 20" Canvas.Bottom="30" Canvas.Left="20"/>
        </Canvas>
    </Grid>
    
    
    
    
</Window>